[project]
name = "workspace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic-settings>=2.7.1",
    "pyinfra>=3.2",
]

[dependency-groups]
dev = [
    "pyright>=1.1.396",
    "ruff>=0.9.10",
]
